# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
.eggs/

# Local env / secrets (never commit)
.env
AGENTS.md

# Sensitive - personal hint sets (candidate password material) and generated
# tokenlists. The sanitized demo hint set is kept.
*hints*.json
!synaptic/examples/demo_hints.json
synaptic_tokens_*.txt

# Sensitive - wallet key files, except the vendored public test fixture
# (password "btcr-test-password", no funds) that the tests/demo need.
*.key
!synaptic/vendor/test-wallets/multibit-wallet.key

# Recovered passwords (written to a 0600 file, never commit)
RECOVERED_PASSWORD.txt

# Internal engineering self-audit (not for publication)
audit/

# OS / editor
.DS_Store
.idea/
.vscode/
