# Pinned dependency set for synaptic. pyproject.toml is the canonical manifest;
# this file mirrors it (exact pins) for the plain `pip install -r` path.
#
# Preferred install:  pip install -e "synaptic[dev]"
# Plain install:      pip install -r synaptic/requirements.txt

# runtime
pycryptodome==3.23.0

# dev / CI
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.0

# optional: YAML hint files
# pyyaml==6.0.2
