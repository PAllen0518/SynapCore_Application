# synaptic runtime + test dependencies.
#
# The package itself uses only the Python standard library to talk to SynapCores.
# pycryptodome is needed because synaptic reuses BitCracker's multibit_check for
# candidate enumeration and in-process checking. pytest/ruff are for CI.
pycryptodome>=3.20
pytest>=8.0
ruff>=0.6.0
# Optional: PyYAML lets hint sets be written as .yml instead of .json.
# pyyaml>=6.0
